import { getAllConfesion, loginByCredentials } from "./service"


export default async () => {
   console.log('EJECUTANDO TESTS 2')
   // const response = await loginByCredentials({
   //    password: 'brandon12',
   //    username: 'brandox'
   // })
   
   // console.log(response.data.token)
}